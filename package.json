{
  "name": "pollon-sjr",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "build:api:production": "prisma generate --schema=packages/prisma/schema.prisma && node scripts/build-api.cjs",
    "test:api:compiled": "node scripts/test-api-compiled.cjs",
    "test:api:runtime": "node scripts/test-api-runtime.cjs",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:generate": "npm run generate --workspace=packages/prisma",
    "db:migrate": "npm run migrate --workspace=packages/prisma",
    "db:seed": "npm run seed --workspace=packages/prisma"
  },
  "dependencies": {
    "tsx": "^4.10.0"
  },
  "devDependencies": {
    "esbuild": "0.27.7",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "prisma": "^5.14.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
