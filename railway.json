{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate --schema=packages/prisma/schema.prisma && npm run build --workspace=apps/api"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=packages/prisma/schema.prisma && node apps/api/dist/server.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
